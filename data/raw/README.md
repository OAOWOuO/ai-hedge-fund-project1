# data/raw — Course Materials

Drop your course PDFs and case documents here, then build the index:

    python scripts/build_index.py

Supported formats: `.pdf`, `.md`, `.txt`

> Note: PDF and TXT files in this directory are git-tracked.
> The global `*.pdf` / `*.txt` ignore rules are overridden for this path
> in `.gitignore`. Do not commit confidential materials.
